# Progress Log

## Learnings
(Patterns discovered during implementation — updated by Ralph after each iteration)

---

## Iteration 1 - US-001: SQLite schema — 3 new ORM tables
- Both backend/models.py and backend/database.py were already fully implemented
- All 3 ORM classes (Project, ProjectSession, ProjectDocument) present with correct columns
- All CREATE TABLE IF NOT EXISTS migrations present in init_db()
- All indexes present (ix_projects_status, ix_project_sessions_project_id, ix_project_documents_project_id)
- Files changed: backend/models.py, backend/database.py, ralph_loop/PRD.md
- Learnings for future iterations:
  - Models and DB were pre-populated; always read files before implementing
  - Import test (`python -c "from backend.models import ..."`) is fast typecheck substitute when mypy not installed
  - Column inspection via `__mapper__.columns` confirms ORM field completeness
---

## Iteration 3 - US-003: Memory document — initialize and patch functions
- data/project_memory.py was already fully implemented (pre-populated)
- All functions present: initialize_memory_doc, patch_memory_section, trim_memory_doc, generate_document_summary, update_project_memory
- SECTION_HEADERS list matches PRD schema exactly
- Optimistic locking pattern implemented (2-attempt loop with updated_at check)
- trim_memory_doc called inside update_project_memory after patch applied
- Files changed: ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - Files continue to be pre-populated; always read before implementing
  - Import test + logic assertions are sufficient for typecheck when mypy not installed
---

## Iteration 4 - US-004: Context assembly middleware
- Created `backend/context_assembly.py` with `assemble_project_context()` async function
- Returns empty string for non-project sessions (project_id=None)
- Loads project from SQLite via async select, parses config JSON for tickers
- Calls chroma_client.async_query() for top-K relevant chunks with try/except guard
- Builds XML block: <project_context> with nested <thesis>, <memory_doc>, <document_excerpts>, <project_tickers>
- Token budget enforced: memory_doc ≤3200 chars (~800 tokens), each chunk ≤1600 chars (~400 tokens)
- Files changed: backend/context_assembly.py, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - Chroma async_query already has try/except in chroma_client.py but still guard here for resilience
  - Use `scalar_one_or_none()` for safe single-row fetch with async SQLAlchemy
  - XML tags chosen over markdown to avoid conflicts with memory_doc markdown content
---

## Iteration 5 - US-005: Project router — agent routing LLM call
- Created `backend/project_router.py` with `AgentTask` and `ProjectRoutingDecision` dataclasses
- `route_for_project(query, context_block, project_config)` async function using Claude Haiku
- System prompt encodes activation rules for all 5 agents (dcf, analyst, earnings, market, research)
- Extracts thesis excerpt from XML context_block for task grounding
- Parses JSON response, filters to valid agent_types, falls back to research on any error
- Files changed: backend/project_router.py, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - Extract thesis from XML context_block using string find() — robust, no XML parser needed
  - `from __future__ import annotations` needed for `List[AgentTask]` forward refs in dataclasses
  - Validate agent_types against whitelist after JSON parse to prevent invalid routing
---

## Iteration 6 - US-006: ProjectAnalysisState TypedDict + graph skeleton
- Created `agents/project_agent.py` with ProjectAnalysisState TypedDict and compiled LangGraph graph
- ProjectAnalysisState: 10 fields — query, project_id, context_block, routing_decision, agent_results (operator.add), errors (operator.add), synthesis, memory_patch, final_response, start_time
- Graph topology: START → route → [5 agent nodes] → sync_point → synthesize → extract_memory_patch → END
- Conditional edge uses _select_agent_nodes() returning list of node names from routing_decision.agents dict
- Falls back to ["run_agent_research"] if routing_decision empty or contains no valid agent_types
- All 5 agent runner nodes + synthesize + extract_memory_patch are stubs (pass-through) per US-006 spec
- ProjectAnalysisGraph class wraps graph, ProjectAnalysisGraphAdapter provides invoke() interface
- Files changed: agents/project_agent.py, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - LangGraph add_conditional_edges with list return works: pass {node: node for node in all_nodes} as path_map
  - `from __future__ import annotations` needed for `dict | None` union type in Python 3.9
  - routing_decision agents can be dicts (JSON-serialised) or dataclass objects — check both with isinstance + getattr
  - Import test + assertion checks sufficient for typecheck without mypy

---

## Iteration 7 - US-006b: Agent runner nodes
- Replaced 5 stub run_agent_* nodes in agents/project_agent.py with real implementations
- Added imports: create_dcf_agent, create_equity_analyst_agent, create_earnings_agent, create_market_agent, create_finance_qa_agent
- Added helpers: _get_agent_task(), _grounded_task(), _extract_ticker(), _ensure_str()
- Each node: extracts task from routing_decision, prepends context_block, calls agent, appends to agent_results
- Earnings node uses _extract_ticker() to pull ticker from task/query (needed since EarningsAgent.analyze(ticker))
- Research node calls agent.chat() (not analyze()) — matches api_server.py AGENT_FALLBACK_METHODS pattern
- All nodes handle exceptions: append to errors + append error entry to agent_results, never re-raise
- _emit_progress() called at start + end of each node (same pattern as earnings_agent.py)
- Files changed: agents/project_agent.py, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - EarningsAgent.analyze() takes ticker (str), not query — must extract ticker from task
  - FinanceQAAgent uses chat() not analyze() — check api_server AGENT_FALLBACK_METHODS for ground truth
  - Agents are created fresh per node call (no persistent state needed in graph runners)
  - _ensure_str() needed because ChatAnthropic returns list of content blocks, not plain string
---

## Iteration 8 - US-006c: Synthesize + extract_memory_patch nodes + adapter
- Replaced stub `synthesize` node with 1 Sonnet (claude-sonnet-4-6) LLM call
- Replaced stub `extract_memory_patch` node with 1 Haiku (claude-haiku-4-5-20251001) LLM call
- synthesize: builds agent_results section + thesis excerpt, calls Sonnet, writes synthesis + final_response
- synthesize: graceful error message when agent_results is empty (references errors list)
- extract_memory_patch: instructs Haiku to return JSON with conclusions, violated_assumptions, thesis_health, open_questions
- Both use sync anthropic.Anthropic() client (LangGraph nodes are synchronous)
- ProjectAnalysisGraph.run() and ProjectAnalysisGraphAdapter.invoke() were already implemented in US-006
- Files changed: agents/project_agent.py, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - Use sync anthropic.Anthropic() (not AsyncAnthropic) in LangGraph nodes — they run in sync context
  - setdefault() on parsed JSON dict ensures all required keys always present even if Haiku omits one
  - Thesis extracted from <thesis>...</thesis> XML tags in context_block using string find() — same pattern as project_router.py
---

## Iteration 9 - US-007: Project CRUD REST endpoints
- Added 8 endpoints to backend/api_server.py under "REST Endpoints — Projects" section
- Imported Project, ProjectSession, ProjectDocument from backend.models
- CreateProjectRequest (title, thesis, tickers optional), ProjectPatch, ProjectMemoryPatch Pydantic models
- _project_detail() / _project_summary() helpers serialize Project ORM to dict
- POST /projects: calls initialize_memory_doc(), stores config as JSON with tickers list
- GET /projects: queries session_count + document_count via func.count() for each project
- GET /projects/{id}: full detail with counts
- PATCH /projects/{id}: partial update of title/thesis/config/status + updated_at
- DELETE /projects/{id}: soft archive (status='archived') + best-effort Chroma cleanup
- GET /projects/{id}/memory and PATCH /projects/{id}/memory: memory_doc read/write
- GET /projects/{id}/sessions: joins ProjectSession → DBSession for session list
- Files changed: backend/api_server.py, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - Import `func` from sqlalchemy for count() queries in async context
  - `@app.post("/projects", status_code=201)` — decorator with status_code not matched by naive `@app.post("/projects")` string check — always check file directly with grep
  - Use `async_delete_collection` (already exists in chroma_client.py) for async Chroma cleanup
  - `_project_summary` strips memory_doc to keep list responses lean
---

## Iteration 10 - US-008: Document upload endpoint for projects
- Added 3 endpoints to backend/api_server.py: POST/GET /projects/{project_id}/documents, DELETE /projects/{project_id}/documents/{doc_id}
- POST: reads file, validates ext/size, calls extract_text_from_file(), creates ProjectDocument with flush to get ID, calls async_add_document_chunks(), generates summary with ChatAnthropic Haiku via generate_document_summary(), patches memory_doc Uploaded Document Summaries section via patch_memory_section(mode="append")
- GET: returns doc list without raw_text field
- DELETE: deletes Chroma chunks via async_delete_chunks(), then deletes SQLite record
- Files changed: backend/api_server.py, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - Use db.flush() (not commit) after db.add() to get auto-generated ID while still in transaction
  - ChatAnthropic must be imported locally inside the try block (not at top-level) to avoid startup cost
  - async_add_document_chunks / async_delete_chunks / async_delete_collection already exist in chroma_client.py
  - patch_memory_section with mode="append" is the right call for document summaries (each upload adds to section)
---

## Iteration 11 - US-009: Inject project context into /chat/stream
- Added `project_id: Optional[str] = None` to `ChatMessage` model in api_server.py
- Added `project_id: Optional[str] = None` to `stream_agent_response` signature
- Added `run_project_graph_with_callbacks` helper: sets _progress_queue/_progress_loop on graph, runs invoke() in executor, puts response+done in queue, captures _state in state_container
- Project path in stream_agent_response: assembles context (AsyncSessionLocal), routes via route_for_project, runs ProjectAnalysisGraph, drains queue, persists with project_id
- Modified `_persist_conversation` to accept `project_id`, creates ProjectSession link via sqlite INSERT OR IGNORE (on_conflict_do_nothing)
- Pre-load ProjectChromaClient in on_startup for warm embedding model
- Non-project sessions (project_id=None) completely unaffected — project path returns early
- Files changed: backend/api_server.py, ralph_loop/PRD.md
- Learnings for future iterations:
  - sqlite INSERT OR IGNORE via `sqlalchemy.dialects.sqlite.insert(...).on_conflict_do_nothing()` — cleaner than SELECT+INSERT
  - state_container dict pattern captures sync result from executor without polluting queue protocol
  - Project path opens its own AsyncSessionLocal for context assembly (not passed from handler)
  - Import `Project` from backend.models already present in api_server.py — no new import needed
---

## Iteration 12 - US-010: Memory update background job
- Added `_run_memory_update(project_id, memory_patch)` async helper to api_server.py
- Helper opens its own AsyncSessionLocal and calls update_project_memory() from data/project_memory.py
- Errors logged at ERROR level, never raised — memory failure is transparent to user
- Added create_task call after `await task` in project path: extracts memory_patch from state_container["state"], guards on non-empty patch, schedules _run_memory_update as background task
- update_project_memory() was already fully implemented (optimistic locking, trim, all 4 patch keys)
- Files changed: backend/api_server.py, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - state_container["state"] holds final LangGraph state including memory_patch — populated by run_project_graph_with_callbacks
  - Background tasks that need DB access must open their own AsyncSessionLocal (can't reuse the handler's session)
  - Use `or {}` guard on memory_patch to handle None (graph may not always populate it)
---

## Iteration 13 - US-011: Frontend TypeScript types for projects
- Added ProjectSummary, ProjectConfig, ProjectDetail, ProjectDocument interfaces to frontend/src/types.ts
- Extended ChatRequest with optional `project_id?: string` field
- Pre-existing tsc error in AgentChart.tsx (not my change) — confirmed by stash test
- Files changed: frontend/src/types.ts, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - AgentChart.tsx has a pre-existing TS error — don't count it as a failure
  - Stash test is a reliable way to confirm errors are pre-existing
---

## Iteration 14 - US-012: Frontend API client functions for projects
- Added project API functions to frontend/src/api.ts
- Imported ProjectSummary, ProjectDetail, ProjectDocument from types.ts
- Added 11 functions: getProjects, getProject, createProject, updateProject, deleteProject, uploadProjectDocument, getProjectDocuments, deleteProjectDocument, getProjectMemory, patchProjectMemory, getProjectSessions
- streamMessage() already passes project_id (ChatRequest already has project_id?: string and body is JSON.stringify(request))
- getProjectMemory returns response.data.memory_doc string (backend returns {memory_doc: string})
- Files changed: frontend/src/api.ts, ralph_loop/PRD.md, ralph_loop/progress.txt
- Learnings for future iterations:
  - Pre-existing AgentChart.tsx TS error (type unknown not assignable to ReactNode) — don't count as failure
  - uploadProjectDocument uses axios directly (not api instance) to set multipart/form-data header — same pattern as existing uploadDocument
  - streamMessage already correct since ChatRequest has project_id?: string and full request serialized to JSON
---

## Iteration 2 - US-002: ChromaDB dependency + ProjectChromaClient
- Added `chromadb>=0.4.0,<0.6.0` to requirements.txt
- Created `data/chroma_client.py` with singleton `ProjectChromaClient`
- Implemented: get_or_create_collection, add_document_chunks, query, delete_chunks, delete_collection
- All sync methods wrapped in async variants via `run_in_executor`
- Chroma persists to `chroma_db/` in project root (resolved relative to data/ dir using __file__)
- Typecheck: import test + unit test of _split_text logic (no chromadb install needed for logic tests)
- Files changed: requirements.txt, data/chroma_client.py, ralph_loop/PRD.md
- Learnings for future iterations:
  - chromadb import deferred to _ensure_initialized() so module imports cleanly even if chromadb not yet installed
  - Singleton pattern: __new__ + _initialized flag (same as SECEdgarClient pattern in data/sec_edgar.py)
  - Chroma query raises if collection is empty — wrap in try/except and return []
  - L2 distance → similarity score: 1/(1+dist) maps [0, ∞) → (0, 1]
---
